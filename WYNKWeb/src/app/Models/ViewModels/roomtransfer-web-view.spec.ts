import { RoomtransferWebView } from './roomtransfer-web-view';

describe('RoomtransferWebView', () => {
  it('should create an instance', () => {
    expect(new RoomtransferWebView()).toBeTruthy();
  });
});
